<catelControls:Page
    x:Class="CatelTest.Uwp.Ui.Views.PersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CatelTest.Uwp.Ui.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:catelControls="using:Catel.Windows.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="__catelInnerWrapper" >
        <StackPanel>
            <TextBox x:Name="TextBoxFirstName" Text="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="Firstname" />
            <TextBox x:Name="TextBoxSurnames" Text="{Binding Surnames, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="Surnames" />
            <CheckBox IsChecked="{x:Bind VM.IsChecked, Mode=TwoWay}" />
            <Button x:Name="ButtonForceUpdate" Content="Force update" Command="{Binding ForceUpdateCommand}" />
        </StackPanel>
        <TextBlock x:Name="TextBoxFullname" Text="{x:Bind VM.Fullname, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Center" Style="{ThemeResource HeaderTextBlockStyle}" />
    </Grid>
</catelControls:Page>
